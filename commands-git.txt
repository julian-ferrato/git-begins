git clone « lien du repository »

git add « nom du fichier »
git commit -m «description »
git push -u origin main

git pull ou git pull all

git branch "nom de la branche"
git checkout "nom de la branche"
